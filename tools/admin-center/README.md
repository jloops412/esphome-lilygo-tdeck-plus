# Admin Center v1

Open `index.html` in a browser to generate:

1. a drop-in install YAML
2. an entity/substitution overrides block

This is an install-time config helper. Runtime controls are still provided by ESPHome entities/web server in firmware.

