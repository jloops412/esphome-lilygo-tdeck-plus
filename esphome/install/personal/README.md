# Personal Profiles

This folder is for user-specific install and entity files that are not used as public defaults.

- Public install/template files live directly under `esphome/install/`.
- Personal examples live under `esphome/install/personal/<profile>/`.

Current profile:

- `jloops/lilygo-tdeck-plus-install-lvgl.yaml`
- `jloops/entity-overrides.yaml`

